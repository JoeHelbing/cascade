# Documentation

Start here when reading the repository manually.

- [`repository/`](repository/) — map of the cleaned-up repository layout and what belongs where, including the [cleanup report](repository/cleanup-report-2026-05-03.md).
- [`verification/`](verification/) — detailed guide to the Mesa → Mojo CPU → Mojo GPU verification pipeline.
- [`paper/`](paper/) — paper/thesis source material currently stored in the repository.

The code-facing READMEs remain at the root, `autoresearch/`, `autoresearch/validation/`, and `original_python/`.
