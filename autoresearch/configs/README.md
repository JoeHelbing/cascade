# Configs

Small checked-in parameter/configuration files used by sweeps and targeted analysis.

| File | Purpose |
|---|---|
| `agent_sim_params.json` | Phase 1F per-agent simulation target set. Generated by `../sweeps/generate_agent_params.py`. |

Large generated outputs do not belong here; they should remain ignored under result/output directories.
